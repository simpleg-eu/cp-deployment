apiVersion: v1
kind: Service
metadata:
  name: cp-config-service
spec:
  selector:
    app: cp-config
    env: production
  ports:
    - protocol: TCP
      port: 1080
      targetPort: 80
  type: LoadBalancer